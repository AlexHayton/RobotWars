var Enum = require('enum');

// Direction Enum
// --------------

// This enum represents the four compass points, which are the possible values
// for robots' directions.

module.exports = new Enum(['North', 'East', 'South', 'West']);